#include <stdio.h>
#include <math.h>

int main() {
    double num_arrangements = 1;
    for (int i = 25; i > 0; i--) {
        num_arrangements *= i;
    }

    double num_symmetries = pow(2, 6);

    double num_keys = num_arrangements / num_symmetries;

    double log2_num_keys = log2(num_keys);

    printf("Approximate number of possible keys: 2^%.0f\n", log2_num_keys);

    return 0;
}
